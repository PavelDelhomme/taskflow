# Hubera Tasks

Ce repo (TaskFlow) **est** le produit Hubera Tasks (`tasks.hubera.cloud`).

L’ancien `Cloudity/backend/tasks-service` (et `mobile/tasks`) sera **fusionné ici** (port, pas un second produit). Jusque-là le service Cloudity reste dans l’archive.

Ne pas `down -v`. Conteneur prod actuel : `taskflow:3003`.
